Request → Route → authMiddleware → isAdmin → Controller → Service → Repository → Model → DB
                                                  ↓
                                          notifyAdmins() → Socket.io → all admins